#include <stdio.h> //Pre-processor directive - It is basically a library / module (like in js). We can use functions like "printf" with the help of this thing.

int main(){

    printf("Hello, World!");

    return 0; //This marks the ending of the function
}
